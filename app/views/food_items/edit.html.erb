<h1>Editing Food Item</h1>
 
<%= form_for :food_item, url: food_item_path(@food_item), method: :patch do |f| %>
 
  <% if @food_item.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@food_item.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @article.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
 
  <p>
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </p>

  <p>
    <%= f.label :amount %><br>
    <%= f.text_area :amount %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
 
<% end %>
 
<%= link_to 'Back', food_item_path %>
